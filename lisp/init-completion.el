;;; init-completion.el --- Minibuffer & in-buffer completion -*- lexical-binding: t -*-

;; Placeholder for completion stack (e.g. vertico + orderless + consult + corfu)

(provide 'init-completion)
;;; init-completion.el ends here
